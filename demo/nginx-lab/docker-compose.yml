services:
  nginx:
    image: nginx:1.27-alpine
    container_name: aegis-nginx-lab
    ports:
      - "18080:80"
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./www:/usr/share/nginx/html:ro
    restart: "no"
